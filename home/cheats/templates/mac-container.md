---
syntax: sh
tags: [ {{TAGS}} ]
---
# Command {{COMMAND_NAME}}

## What does this command do:

{{SUMMARIZE}}

## Installed from

For Mac container (Fedora Linux, ARM/aarch64) install using: {{INSTALL_METHOD_MAC_CONTAINER}}

> Note: the Mac container runs Fedora Linux on ARM (aarch64), not x86_64. When a
> package is not installed via `dnf`, download the `aarch64`/`arm64` build of the
> binary — never the `x86_64`/`amd64` build used on the Fedora workstation.

## Command path

For Mac container run: {{COMMAND_PATH_MAC_CONTAINER}}

## Command documentation

Docs can be found at {{DOCUMENTATION_URL}}

## Command options

TBD

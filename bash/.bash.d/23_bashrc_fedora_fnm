# ~/.bash.d/23_bashrc_fedora_fnm
# Initialize fnm (Fast Node Manager) if installed
if command -v fnm &>/dev/null; then
    if [ -n "${ZSH_VERSION:-}" ]; then
        eval "$(fnm env --use-on-cd --shell zsh)"
    else
        eval "$(fnm env --use-on-cd)"
    fi
fi
